var _ = require('underscore');
var Backbone = require('backbone');
var cookie = require('./cookie');
var config = require('./config.js');

module.exports = function() {
  Backbone.GitHub = function(method, model, options) {
    debugger;
  };

  return Backbone.GitHub;
};
